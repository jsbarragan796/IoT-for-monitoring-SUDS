{
  "name": "suds-dev",
  "version": "1.0.0",
  "description": "## Introduction",
  "main": "index.js",
  "scripts": {
    "first-deploy-local": "cd aggregate-data && npm run first-deploy && cd ../crude-data && npm run first-deploy && cd ../event-manager && npm run first-deploy && cd ../historical-viz && npm run first-deploy && cd ../notification-server && npm run first-deploy && cd ../reception && npm run first-deploy",
    "deploy-local": "cd aggregate-data && npm run deploy && cd ../crude-data && npm run deploy && cd ../event-manager && npm run deploy && cd ../historical-viz && npm run deploy && cd ../notification-server && npm run deploy && cd ../reception && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jsbarragan796/backend.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jsbarragan796/backend/issues"
  },
  "homepage": "https://github.com/jsbarragan796/backend#readme",
  "dependencies": {
    "pm2": "^3.2.2"
  },
  "devDependencies": {
    "eslint": "^5.12.0",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-node": "^8.0.1",
    "eslint-plugin-promise": "^4.0.1",
    "eslint-plugin-standard": "^4.0.0"
  }
}
